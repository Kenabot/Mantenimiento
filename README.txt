Un simple bot de Discord que responde a cualquier interacción con un mensaje de mantenimiento.
Con soporte para múltiples clientes e idiomas.

Para usarlo, simplemente renombra el archivo `config/tokens.ts.e` a `config/tokens.ts` y rellena los tokens.